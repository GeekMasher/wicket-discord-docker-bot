# Domain when the Service can be accessed (domain or IP)
domain: domain.example.com

# List of Admins from any Discord servers / channel
admins:
  # - MyUser#1234

# Discord servers for better fine grain control
servers:
  # example:
  #   name: Example Server Name
  #   admins:
  #     - MyUser#1234

docker-labels:
  - wicket.services.type=gaming
